# TS-DFM — 项目工作约定

## 开始工作

- 开始项目任务前，读取公共平台约定 `/inspire/qb-ilm/project/chemicalreaction/czxs25220150/AGENTS.md`；如果已在上下文中加载，无需重复读取。独立 Git 仓库不保证自动继承上层文件，因此保留这个显式入口。
- 再读取本项目 `docs/STATUS.md` 和 `git status --short`，确认当前目标、验证边界和已有修改。文件中的命令、历史状态以当前代码和实际检查为准。
- 公共文件不可访问时说明情况，不假定已经加载；移植到其他机器时更新引用路径。

## 文档维护

- 完成实质性工作时同步必要记录：稳定约定更新到 `AGENTS.md`，当前进度、验证结果、问题和下一步更新到 `docs/STATUS.md`。普通问答无需写文件。
- 只有实际执行并通过的检查才能标为已验证；记录日期、命令、结果与限制。推测标为待验证，过期结论应修正，不无限追加对话流水账。
- 开展实验时按需建立 `docs/EXPERIMENTS.md` 或独立实验记录，记录代码版本及未提交修改、配置、数据划分、种子、镜像/环境、任务 ID、指标和产物路径。正在运行的状态须标注查询时间。
- 新任务接续当前进度，不自动重跑已完成实验。多个任务并行工作时避免覆盖彼此的记录；不同实验优先写独立记录。
- 文档不存密码、cookie、token；运行产物和大型数据不因文档初始化而加入版本管理。

## 项目说明与入口

- TS-DFM 使用距离几何上的流模型预测过渡态。先读 `README.md`；复现实验相关工作还需读 `docs/reproduction_audit.md`。
- 主训练入口为 `Scripts/train_flow_matching_dist_ts1x.py`。论文 Transition1x 配置为 `Configs/Dynamics.yml`，混合 RGD1 + Transition1x 配置为 `Configs/Dynamics_mixed.yml`。其他脚本入口在 `run_scripts/`。
- 推理/评估入口为 `Scripts/infer_ts1x_xyz.py`、`Scripts/evaluate_ts1x_xyz.py`；坐标重建检查为 `Scripts/check_ts1x_reconstruction_oracle.py`。使用前核对脚本参数、HDF5 和 checkpoint 路径。
- 距离矩阵训练损失不等于最终坐标 RMSD/DMAE。报告论文指标时，需完成对应 XYZ 生成和评估。
- 论文复现按 README 保持 batch size 32 等配置；调整实验定义时单独记录。混合数据实验不能直接当作 Transition1x-only 的论文复现结果。
- 仓库包含其他方法的基线代码目录；修改前区分主 TS-DFM 与内嵌基线，不能拿某个基线的依赖文件当作整个仓库的环境定义。
- 数据路径和划分以 `Data/`、选定配置及现有审计文档为依据。环境、测试入口和镜像尚需实查，不能仅凭文档声称运行成功。日志与 checkpoint 位置按当前配置核对。

## 实验资源偏好

- 用户于2026-09-06明确要求：今后实验尽量使用账号/规格允许的最高优先级；用户确认当前最高优先级为6，使用 `--priority 6`；提交后以平台实际priority_level/priority_name核实，不把旧CLI帮助中的10当作最高档。已验证实验无需重复smoke，仅对新增路径做必要验证。
- 当前RGD1 R→P可用4/8卡H100/H200；优先从有效checkpoint恢复，仅恢复不可行时从头训练。

- 用户于2026-09-06提醒：分布式训练连续4小时GPU利用率低于60%会被回收。启动后检查真实GPU利用率与吞吐，不用资源分配比例代替计算利用率；平台实际生效规则仍需按当前配置核实。
- 当前分布式空间为公平空间，网页优先级只开放1/4，项目最高HIGH-6会映射为NORMAL-4；当前尽量用4，其他非公平空间按账号允许上限（用户确认当前6）。